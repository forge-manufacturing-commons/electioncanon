// ============================================================
// ELECTIONCANON EXTRACTION — INERT COMPATIBILITY STUB.
// See src/domains/production/state.js's header for the full explanation.
// The real file (in the fatt-app monorepo this was extracted from)
// holds Forge-A-Truck's manufacturing-network organisation registry —
// real partner/organisation data that has nothing to do with
// ElectionCanon and must not appear in this repository.
// ============================================================

export const COMPONENT_CLASS = Object.freeze({});
export const SEED_ORGANISATIONS = Object.freeze([]);

export default { COMPONENT_CLASS, SEED_ORGANISATIONS };
